/**
 * Optional: Tastenkürzel zum Öffnen der Quelldatei auf GitHub.
 * Platzhalter, falls das Feature nicht konfiguriert ist — verhindert Build-Fehler.
 */
export function registerGithubSourceShortcut(): void {
  // Keine Standardaktion; kann bei Bedarf mit env-gesteuertem Verhalten erweitert werden.
}
