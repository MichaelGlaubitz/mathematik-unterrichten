---
import AlgebraWbThemenKurz from './AlgebraWbThemenKurz.astro';
import UnterthemenClusterGrid from './UnterthemenClusterGrid.astro';
// Themenblock fuer die /themen-Uebersicht.
// View-Modell wird in der Seite vorberechnet (siehe themen/index.astro).
export interface TopicBlockProps {
  slug: string;
  titel: string;
  klassenstufenAnzeige: string;
  einfuehrung: string;
  fehlvorstellungen: string[];
  whiteboard: { frage: string; loesung: string; abbildungFrage?: string; abbildungLoesung?: string }[];
  aufgaben: any[];
  quizzes: any[];
  blog: any[];
  /** Optional: mehrere Links zu Zufallsübungen (z. B. Geometrie: Pythagoras + Strahlensätze). */
  generatorUebungen?: { href: string; label: string }[];
  /** Optional: Link zu einer Zufallsübungsseite (z. B. Pythagoras/Trigonometrie). */
  generatorUebungHref?: string;
  generatorUebungLabel?: string;
  /** Optional: Unterthemen in Clustern (Kurzüberblick zur inhaltlichen Spanne). */
  unterthemenBloecke?: { titel: string; beschreibung?: string; punkte: string[] }[];
}

const {
  slug,
  titel,
  klassenstufenAnzeige,
  einfuehrung,
  fehlvorstellungen,
  whiteboard,
  aufgaben,
  quizzes,
  blog,
  generatorUebungen,
  generatorUebungHref,
  generatorUebungLabel = 'Zufallsaufgaben (Whiteboard)',
  unterthemenBloecke,
} = Astro.props;

const wbThemaMitAkku =
  (slug === 'bruchrechnung' || slug === 'negative-zahlen' || slug === 'algebra') &&
  Boolean(generatorUebungHref) &&
  Boolean(unterthemenBloecke && unterthemenBloecke.length > 0);
const wbThemaAutoHref = generatorUebungHref ? `${generatorUebungHref}?muAuto=1` : '';

---

<article
  id={`thema-${slug}`}
  class:list={[
    'scroll-mt-32 rounded-xl border border-ink-200 bg-surface p-6 md:p-8 mu-dark-glass-panel',
    wbThemaMitAkku && 'mu-wb-unterthemen-root',
  ]}
>
  <header class="mb-4 flex flex-wrap items-baseline gap-3">
    <h3 class="font-serif text-2xl md:text-3xl font-semibold text-ink-900 dark:text-ink-50">
      {titel}
    </h3>
    <span class="badge-accent">{klassenstufenAnzeige}</span>
  </header>

  <p class="prose-mathe text-ink-700 leading-relaxed mb-6 max-w-3xl dark:text-ink-200">
    {einfuehrung}
  </p>

  {unterthemenBloecke && unterthemenBloecke.length > 0 && (
    wbThemaMitAkku ? (
      <details
        class="mu-wb-unterthemen-acc mb-8 rounded-lg border border-ink-200/90 bg-ink-50/90 shadow-sm dark:border-slate-600/50 dark:bg-slate-950/50"
        open={slug === 'algebra'}
      >
        <summary class="cursor-pointer list-none px-4 py-3 text-sm [&::-webkit-details-marker]:hidden">
          <span class="inline-flex items-center gap-1.5">
            <span
              id={`unterthemen-heading-${slug}`}
              class="font-semibold uppercase tracking-wide text-accent-700 dark:text-accent-100"
            >
              Unterthemen
            </span>
            <span
              class="mu-wb-unterthemen-chevron text-ink-500 text-xs font-normal dark:text-ink-400"
              aria-hidden="true"
            >
              ▶
            </span>
          </span>
        </summary>
        <div class="border-t border-ink-200/90 px-4 pb-4 pt-2 dark:border-slate-600/45">
          <p class="text-xs text-ink-600 dark:text-ink-300 mb-3 max-w-3xl">
            Gewünschte Unterthemen oder Stichworte anklicken. Ohne Auswahl werden alle Aufgabentypen gemischt.
          </p>
          <div class="mb-3 flex flex-wrap gap-1.5" role="group" aria-label="Stichwort-Auswahl für alle Unterthemen">
            <button
              type="button"
              class="mu-wb-kw-bulk rounded-md px-2.5 py-1 text-xs font-semibold shadow-sm transition-colors"
              data-mu-wb-kw-bulk="all-select"
              aria-label="Alle Stichwörter auswählen"
            >
              Alle auswählen
            </button>
            <button
              type="button"
              class="mu-wb-kw-bulk rounded-md px-2.5 py-1 text-xs font-semibold shadow-sm transition-colors"
              data-mu-wb-kw-bulk="all-clear"
              aria-label="Alle Stichwörter abwählen"
            >
              Alle abwählen
            </button>
          </div>
          {slug === 'algebra' && generatorUebungHref ? (
            <AlgebraWbThemenKurz generatorUebungHref={generatorUebungHref} />
          ) : (
            <UnterthemenClusterGrid
              slug={slug}
              generatorUebungHref={generatorUebungHref}
              unterthemenBloecke={unterthemenBloecke}
            />
          )}
          <div
            class="mu-wb-thema-stripe mt-4 flex flex-wrap items-center gap-x-3 gap-y-2 rounded-lg border border-ink-200/90 bg-ink-50/90 px-3 py-2.5 text-sm shadow-sm dark:border-slate-600/50 dark:bg-slate-950/50"
            data-mu-wb-auto-href={wbThemaAutoHref}
            data-mu-wb-thema-slug={slug}
            role="region"
            aria-label={`Einstellungen für ${generatorUebungLabel}`}
          >
      <div
        class="inline-flex flex-wrap items-center gap-1.5"
        role="group"
        aria-label="Anzahl Aufgaben"
        title="Wie viele Aufgaben? (klassisch fürs Whiteboard)"
      >
        {
          [5, 10, 15, 20, 30, 40, 50].map((n) => (
            <button
              type="button"
              class="mu-wb-stripe-anzahl rounded-md border px-2.5 py-1 text-xs font-semibold tabular-nums transition-colors sm:px-3 sm:text-sm"
              data-anzahl={n}
            >
              {n}
            </button>
          ))
        }
      </div>
      <span class="hidden h-6 w-px shrink-0 bg-ink-200 dark:bg-slate-600 sm:inline-block" aria-hidden="true" />
      <div class="inline-flex flex-wrap items-center gap-1.5" role="group" aria-label="Darstellung">
        <button
          type="button"
          class="mu-wb-stripe-layout rounded-md border px-2.5 py-1 text-xs font-semibold transition-colors sm:px-3 sm:text-sm"
          data-layout="arbeitsblatt"
          title={`Speichert Arbeitsblatt, öffnet ${generatorUebungLabel} und erzeugt einen neuen Aufgabensatz`}
        >
          Arbeitsblatt
        </button>
        <button
          type="button"
          class="mu-wb-stripe-layout rounded-md border px-2.5 py-1 text-xs font-semibold transition-colors sm:px-3 sm:text-sm"
          data-layout="whiteboard"
          title={`Speichert Whiteboard, öffnet ${generatorUebungLabel} und erzeugt einen neuen Aufgabensatz`}
        >
          Whiteboard
        </button>
      </div>
      <span class="hidden h-6 w-px shrink-0 bg-ink-200 dark:bg-slate-600 sm:inline-block" aria-hidden="true" />
      <label class="flex min-w-0 cursor-pointer items-center gap-2 text-xs text-ink-800 dark:text-ink-200 sm:text-sm">
        <input
          type="checkbox"
          id={`mu-wb-thema-diagrams-${slug}`}
          class="shrink-0 accent-accent-600"
        />
        <span class="min-w-0 leading-snug">
          <span class="font-medium text-ink-900 dark:text-ink-50">Grafiken / Skizzen einblenden</span>
        </span>
      </label>
      </div>
        </div>

      <style is:global>
      /* Tailwind-Utilities auf den Buttons überschreiben — Auswahl muss sichtbar bleiben */
      .mu-wb-unterthemen-root button.mu-wb-thema-kw.mu-wb-thema-kw--an {
        background-color: rgb(91 33 182) !important;
        border-color: rgb(91 33 182) !important;
        color: #fff !important;
      }
      html.dark .mu-wb-unterthemen-root button.mu-wb-thema-kw.mu-wb-thema-kw--an {
        background-color: rgb(167 139 250) !important;
        border-color: rgb(167 139 250) !important;
        color: rgb(15 23 42) !important;
      }
      /* Sammelaktionen: klar von Stichwort-Pills (Akzent-Rand) abgrenzen — gestrichelter Rand, erdige Fläche */
      .mu-wb-unterthemen-root button.mu-wb-kw-bulk {
        border-width: 1px;
        border-style: dashed;
        border-color: #a08f72;
        background-color: #e9e3d5;
        color: #3f352a;
      }
      .mu-wb-unterthemen-root button.mu-wb-kw-bulk:hover {
        background-color: #dcd5c5;
        border-color: #6c5c43;
      }
      html.dark .mu-wb-unterthemen-root button.mu-wb-kw-bulk {
        border-color: #94a3b8;
        background-color: rgb(30 41 59 / 0.72);
        color: #e2e8f0;
      }
      html.dark .mu-wb-unterthemen-root button.mu-wb-kw-bulk:hover {
        background-color: rgb(51 65 85 / 0.85);
        border-color: #cbd5e1;
      }
      /* Kompaktzeile (Farben als RGB/HEX: theme() in Astro-<style> wird nicht aufgelöst) */
      .mu-wb-unterthemen-root .mu-wb-thema-stripe .mu-wb-stripe-anzahl:not(.mu-wb-stripe--aktiv) {
        border-color: #99f6e4;
        background-color: #f0fdfa;
        color: #2a241d;
      }
      .mu-wb-unterthemen-root .mu-wb-thema-stripe .mu-wb-stripe-anzahl:not(.mu-wb-stripe--aktiv):hover {
        background-color: #ccfbf1;
        border-color: #2dd4bf;
      }
      .mu-wb-unterthemen-root .mu-wb-thema-stripe .mu-wb-stripe-layout:not(.mu-wb-stripe--aktiv) {
        border-color: #ddd6fe;
        background-color: #f5f3ff;
        color: #2a241d;
      }
      .mu-wb-unterthemen-root .mu-wb-thema-stripe .mu-wb-stripe-layout:not(.mu-wb-stripe--aktiv):hover {
        background-color: #ede9fe;
        border-color: #a78bfa;
      }
      html.dark .mu-wb-unterthemen-root .mu-wb-thema-stripe .mu-wb-stripe-anzahl:not(.mu-wb-stripe--aktiv) {
        border-color: #0f766e;
        background-color: rgb(19 78 74 / 0.3);
        color: #f3efe6;
      }
      html.dark .mu-wb-unterthemen-root .mu-wb-thema-stripe .mu-wb-stripe-anzahl:not(.mu-wb-stripe--aktiv):hover {
        background-color: rgb(19 78 74 / 0.45);
        border-color: #14b8a6;
      }
      html.dark .mu-wb-unterthemen-root .mu-wb-thema-stripe .mu-wb-stripe-layout:not(.mu-wb-stripe--aktiv) {
        border-color: #7c3aed;
        background-color: rgb(76 29 149 / 0.25);
        color: #f3efe6;
      }
      html.dark .mu-wb-unterthemen-root .mu-wb-thema-stripe .mu-wb-stripe-layout:not(.mu-wb-stripe--aktiv):hover {
        background-color: rgb(76 29 149 / 0.38);
        border-color: #a78bfa;
      }
      .mu-wb-unterthemen-root .mu-wb-thema-stripe .mu-wb-stripe-anzahl.mu-wb-stripe--aktiv,
      .mu-wb-unterthemen-root .mu-wb-thema-stripe .mu-wb-stripe-layout.mu-wb-stripe--aktiv {
        background-color: #2563eb;
        border-color: #2563eb;
        color: #fff;
      }
      html.dark .mu-wb-unterthemen-root .mu-wb-thema-stripe .mu-wb-stripe-anzahl.mu-wb-stripe--aktiv,
      html.dark .mu-wb-unterthemen-root .mu-wb-thema-stripe .mu-wb-stripe-layout.mu-wb-stripe--aktiv {
        background-color: #3b82f6;
        border-color: #2563eb;
        color: #fff;
      }
      .mu-wb-unterthemen-root .mu-wb-unterthemen-acc .mu-wb-unterthemen-chevron {
        display: inline-block;
        transition: transform 0.15s ease;
      }
      .mu-wb-unterthemen-root .mu-wb-unterthemen-acc[open] .mu-wb-unterthemen-chevron {
        transform: rotate(90deg);
      }
      </style>
    </details>
    ) : (
      <section class="mb-8" aria-labelledby={`unterthemen-heading-${slug}`}>
        <h4
          id={`unterthemen-heading-${slug}`}
          class="font-semibold text-ink-900 dark:text-ink-50 mb-3 text-sm uppercase tracking-wide text-accent-700 dark:text-accent-100"
        >
          Unterthemen
        </h4>
        <UnterthemenClusterGrid
          slug={slug}
          generatorUebungHref={generatorUebungHref}
          unterthemenBloecke={unterthemenBloecke}
        />
      </section>
    )
  )}

  {fehlvorstellungen.length > 0 && (
    <details
      class="mu-fehlvorstellungen-acc mb-6 rounded-lg border-l-4 border-accent-500 bg-accent-50 p-4 dark:bg-accent-700/15 dark:border-accent-300"
      data-mu-fehlv-slug={slug}
    >
      <summary class="cursor-pointer list-none text-sm font-semibold text-accent-700 dark:text-accent-100 [&::-webkit-details-marker]:hidden">
        <span class="inline-flex items-center gap-1.5">
          <span>Typische Fehlvorstellungen</span>
          <span class="mu-fehlv-chevron text-ink-500 text-xs font-normal dark:text-ink-400" aria-hidden="true">
            ▶
          </span>
        </span>
      </summary>
      <ul class="mt-3 list-disc space-y-1 pl-5 text-sm text-ink-800 dark:text-ink-100">
        {fehlvorstellungen.map((f) => <li>{f}</li>)}
      </ul>
    </details>
  )}


  <div class="grid gap-5 md:grid-cols-2">
    <details
      class="mu-thema-ressourcen-acc md:col-span-2 rounded-lg border border-ink-200/90 bg-ink-50/90 shadow-sm dark:border-slate-600/50 dark:bg-slate-950/50"
      aria-label="Aufgabenfolgen, diagnostische Fragen und Didaktik-Beiträge"
    >
      <summary class="cursor-pointer list-none px-4 py-3 text-sm font-semibold text-accent-700 dark:text-accent-100 [&::-webkit-details-marker]:hidden">
        <span class="inline-flex items-center gap-1.5">
          <span>ASeq, DiQ, Blogposts</span>
          <span class="mu-thema-ressourcen-chevron text-ink-500 text-xs font-normal dark:text-ink-400" aria-hidden="true">
            ▶
          </span>
        </span>
      </summary>
      <div class="border-t border-ink-200/90 px-4 pb-4 pt-2 dark:border-slate-600/45">
        <div class="grid gap-5 md:grid-cols-2">
          <!-- Aufgaben -->
          <div class="rounded-lg border border-ink-200 p-4 dark:border-slate-600/45 dark:bg-slate-950/30">
            <h4 class="font-semibold text-ink-900 mb-2 dark:text-ink-50 flex items-center gap-2">
              <span aria-hidden="true">📚</span> Aufgabenfolgen
            </h4>
            {aufgaben.length > 0 ? (
              <ul class="space-y-2 text-sm">
                {aufgaben.map((a) => (
                  <li>
                    <a
                      href={`/aufgaben/${a.slug}`}
                      class="text-accent-700 hover:underline dark:text-accent-100"
                    >
                      {a.data.titel}
                    </a>
                    <span class="text-ink-500 dark:text-ink-400">
                      {' '}· Kl. {a.data.klassenstufe.join('/')} · {a.data.schwierigkeit}
                    </span>
                  </li>
                ))}
              </ul>
            ) : (
              <p class="text-sm text-ink-500 dark:text-ink-400 italic">
                Noch keine Aufgabenfolge – kommt.
              </p>
            )}
          </div>

          <!-- Quizzes -->
          <div class="rounded-lg border border-ink-200 p-4 dark:border-slate-600/45 dark:bg-slate-950/30">
            <h4 class="font-semibold text-ink-900 mb-2 dark:text-ink-50 flex items-center gap-2">
              <span aria-hidden="true">🎯</span> Diagnostische Fragen
            </h4>
            {quizzes.length > 0 ? (
              <ul class="space-y-2 text-sm">
                {quizzes.map((q) => (
                  <li>
                    <a
                      href={`/quizzes/${q.id}`}
                      class="text-accent-700 hover:underline dark:text-accent-100"
                    >
                      {q.data.titel}
                    </a>
                    <span class="text-ink-500 dark:text-ink-400">
                      {' '}· Kl. {q.data.klassenstufe.join('/')}
                    </span>
                  </li>
                ))}
              </ul>
            ) : (
              <p class="text-sm text-ink-500 dark:text-ink-400 italic">
                Noch keine diagnostischen Fragen – kommen.
              </p>
            )}
          </div>

          <!-- Blog -->
          <div class="rounded-lg border border-ink-200 p-4 dark:border-slate-600/45 dark:bg-slate-950/30">
            <h4 class="font-semibold text-ink-900 mb-2 dark:text-ink-50 flex items-center gap-2">
              <span aria-hidden="true">✍️</span> Verwandte Didaktik-Beiträge
            </h4>
            {blog.length > 0 ? (
              <ul class="space-y-2 text-sm">
                {blog.map((b) => (
                  <li>
                    <a
                      href={`/blog/${b.slug}`}
                      class="text-accent-700 hover:underline dark:text-accent-100"
                    >
                      {b.data.title}
                    </a>
                    <span class="text-ink-500 dark:text-ink-400">
                      {' '}· {b.data.kategorie}
                    </span>
                  </li>
                ))}
              </ul>
            ) : (
              <p class="text-sm text-ink-500 dark:text-ink-400 italic">
                Noch keine passenden Beiträge verknüpft.
              </p>
            )}
          </div>
        </div>
      </div>
    </details>

    {slug !== 'bruchrechnung' && slug !== 'negative-zahlen' && slug !== 'algebra' && (
    <!-- Mini-Whiteboard (Bruchrechnung, Negative Zahlen, Algebra: Steuerung in der Unterthemen-Kompaktzeile) -->
    <div class="md:col-span-2 rounded-lg border border-ink-200 p-4 dark:border-slate-600/45 dark:bg-slate-950/30">
      <div class="flex items-start justify-between gap-3 mb-2">
        <h4 class="font-semibold text-ink-900 dark:text-ink-50 flex items-center gap-2">
          <span aria-hidden="true">✏️</span> Mini-Whiteboard
        </h4>
        <div class="flex flex-wrap items-center gap-2 shrink-0 justify-end">
          {generatorUebungen && generatorUebungen.length > 0 ? (
            generatorUebungen.map((g) => (
              <a
                href={g.href}
                class="rounded-md border border-accent-600 px-3 py-1 text-xs font-semibold text-accent-700 hover:bg-accent-50 transition dark:border-accent-400 dark:text-accent-100 dark:hover:bg-accent-700/30"
                title="Zufällig erzeugte Aufgaben, Anzahl wählbar"
              >
                {g.label}
              </a>
            ))
          ) : (
            generatorUebungHref && (
              <a
                href={generatorUebungHref}
                class="rounded-md border border-accent-600 px-3 py-1 text-xs font-semibold text-accent-700 hover:bg-accent-50 transition dark:border-accent-400 dark:text-accent-100 dark:hover:bg-accent-700/30"
                title="Zufällig erzeugte Aufgaben, Anzahl wählbar"
              >
                {generatorUebungLabel}
              </a>
            )
          )}
          {whiteboard.length > 0 && (
            <a
              href={`/themen/${slug}/whiteboard`}
              class="inline-flex items-center justify-center rounded-md bg-accent-600 px-3 py-1 text-xs font-semibold text-white hover:bg-accent-700 transition dark:bg-accent-500 dark:hover:bg-accent-600"
              title="Aufgabe für Aufgabe an die Tafel projizieren"
              aria-label={`WB öffnen: ${titel}`}
            >
              <svg xmlns="http://www.w3.org/2000/svg" viewBox="0 0 24 24" class="h-4 w-4" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round" aria-hidden="true">
                <rect x="3.5" y="4.5" width="17" height="11" rx="1.5"></rect>
                <path d="M8 20h8"></path>
                <path d="M12 15.5V20"></path>
                <path d="M7 9h10"></path>
              </svg>
            </a>
          )}
        </div>
      </div>
      {whiteboard.length > 0 ? (
        <ol class="space-y-2 text-sm list-decimal pl-5">
          {whiteboard.map((w) => (
            <li>
              <details class="group">
                <summary class="cursor-pointer text-ink-800 dark:text-ink-100 marker:text-ink-500">
                  {w.frage}
                </summary>
                <p class="mt-1 pl-1 text-ink-700 dark:text-ink-300">
                  <span class="font-semibold text-accent-700 dark:text-accent-100">Lösung:</span>{' '}
                  {w.loesung}
                </p>
              </details>
            </li>
          ))}
        </ol>
      ) : (
        <p class="text-sm text-ink-500 dark:text-ink-400 italic">
          Noch keine Whiteboard-Fragen.
        </p>
      )}
    </div>
    )}

  </div>

  <style is:global>
    .mu-fehlvorstellungen-acc .mu-fehlv-chevron,
    .mu-thema-ressourcen-acc .mu-thema-ressourcen-chevron {
      display: inline-block;
      transition: transform 0.15s ease;
    }
    .mu-fehlvorstellungen-acc[open] .mu-fehlv-chevron,
    .mu-thema-ressourcen-acc[open] .mu-thema-ressourcen-chevron {
      transform: rotate(90deg);
    }
  </style>
</article>
