---
export interface Props {
  href: string;
  titel: string;
  teaser: string;
  meta?: string;
  badges?: string[];
  datum?: Date;
}
const { href, titel, teaser, meta, badges = [], datum } = Astro.props;
const datumString = datum
  ? new Intl.DateTimeFormat('de-DE', { day: 'numeric', month: 'long', year: 'numeric' }).format(datum)
  : '';
---

<a href={href} class="card flex flex-col gap-3 group">
  <div class="flex flex-wrap items-center gap-2 text-xs">
    {badges.map((b) => (
      <span class="badge-accent">{b}</span>
    ))}
    {datum && <time class="text-ink-500" datetime={datum.toISOString()}>{datumString}</time>}
  </div>

  <h3 class="font-serif text-xl font-semibold text-ink-900 group-hover:text-accent-700 transition-colors">
    {titel}
  </h3>

  <p class="text-ink-700 leading-relaxed text-[0.95rem]">{teaser}</p>

  {meta && <p class="text-xs text-ink-500 mt-auto">{meta}</p>}
</a>
