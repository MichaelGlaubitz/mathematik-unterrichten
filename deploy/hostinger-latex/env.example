# Öffentliche Adresse, unter der Caddy ein Zertifikat ausstellt (nur Hostname, ohne https://).
# Beispiel: latex.ihredomain.de — DNS A-Record muss auf die VPS-IP zeigen (z. B. 72.62.x.x).
SITE_ADDRESS=latex.ihredomain.de
